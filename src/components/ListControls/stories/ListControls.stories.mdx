import {
  ArgsTable,
  Meta,
  Canvas,
  Story,
  Source,
} from '@storybook/addon-docs/blocks';
import ListControls from '../ListControls';
import styles from './story.scss';
import dedent from 'ts-dedent';

<Meta
  title="Components/ListControls"
  component={ListControls}
  argTypes={{
    itemsPerPage: {
      control: {
        type: 'select',
        options: [5, 10, 20, 50, 100],
      },
    },
    selectedSort: {
      control: {
        type: 'select',
        options: [
          'Title A-Z',
          'Title Z-A',
          'Date: New to Old',
          'Date: Old to New',
        ],
      },
    },
  }}
/>

## ListControls

Component to control the layout in a list of items (to show item in a list in a vertical or horizontal layout) and configure the number of items per page.

<Canvas>
  <Story
    name="Basic"
    args={{
      orientation: 'vertical',
      onLayoutChange: (orientation) => null,
      onItemsPerPageChange: (value) => null,
      itemsPerPage: 5,
      className: styles.listControlsContainer,
      sortOptions: [
        { id: 0, label: '-' },
        { id: 1, label: 'Title A-Z' },
        { id: 2, label: 'Title Z-A' },
        { id: 3, label: 'Date: New to Old' },
        { id: 4, label: 'Date: Old to New' },
      ],
      selectedSort: 2,
    }}
  >
    {(args) => <ListControls {...args} />}
  </Story>
</Canvas>

## Props

<ArgsTable of={ListControls} />

## ClassNames you can override

You can pass a className to the root element to customize styles:

<Source
  language="js"
  code={dedent`<ListControls className="my-class-name" />`}
/>

Also, you can override styles using specific class names:

```
// Class for each select element
.Sui-ListControls--container {...}
```
