import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Source,
} from '@storybook/addon-docs/blocks';
import FitmentStoreWrapper from '../FitmentStoreWrapper';
import dedent from 'ts-dedent';
import { ImportStylesText } from '../../../stories/ImportStylesText.mdx';
import styles from './story.scss';
import { FitmentStoreWrapperComp, ItemComp } from './NestedInterfaces';

<Meta title="Wrappers/FitmentStoreWrapper" component={FitmentStoreWrapper} />

export const Template = (args) => <FitmentStoreWrapper {...args} />;

# FitmentStoreWrapper

`FitmentStoreWrapper` let users manage fitment selector for quick and easy browsing.

This component shows previous selected fitments and user can add more fitments, delete one or clear the history.

## Basic

<Canvas>
  <Story name="Basic" args={{ htmlSection: () => <h3>No results</h3> }}>
    {(args) => {
      return <FitmentStoreWrapper {...args} className={styles.story} />;
    }}
  </Story>
</Canvas>

## Styled

<Canvas>
  <Story name="Styled" args={{ styled: true }}>
    {(args) => {
      return <FitmentStoreWrapper {...args} className={styles.story} />;
    }}
  </Story>
</Canvas>

<ImportStylesText componentName="FitmentStoreWrapper" />

## Props FitmentStoreWrapper

<ArgsTable of={FitmentStoreWrapper} />

## FitmentStoreWrapper

<ArgsTable of={FitmentStoreWrapperComp} />

## Item

<ArgsTable of={ItemComp} />

## ClassNames you can override

You can pass a className to the root element to customize styles:

<Source
  language="js"
  code={dedent`<FitmentStoreWrapper className="my-class-name" />`}
/>

Also, you can override styles using specific class names:

```
.Sui-FitmentStore--title
.Sui-FitmentStore--subtitle
.Sui-FitmentStore--popover-container
.Sui-FitmentStore--popover-title
.Sui-FitmentStore--popover-list
.Sui-FitmentStore--delete-fitment-btn
```
