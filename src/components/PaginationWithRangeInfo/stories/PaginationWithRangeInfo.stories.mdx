import {
  ArgsTable,
  Meta,
  Canvas,
  Story,
  Source,
} from '@storybook/addon-docs/blocks';
import PaginationWithRangeInfo from '../PaginationWithRangeInfo';
import dedent from 'ts-dedent';

<Meta
  title="Components/PaginationWithRangeInfo"
  component={PaginationWithRangeInfo}
  argTypes={{
    itemsPerPage: {
      control: {
        type: 'select',
        options: [5, 10, 20, 50, 100],
      },
    },
  }}
/>

## PaginationWithRangeInfo

Component to show pagination with current selected page ranges (from - to).

<Canvas>
  <Story
    name="Basic"
    args={{
      totalItems: 293,
      currentPage: 1,
      itemsPerPage: 5,
      onChangePage: (page) => null,
    }}
  >
    {(args) => <PaginationWithRangeInfo {...args} />}
  </Story>
</Canvas>

## Props

<ArgsTable of={PaginationWithRangeInfo} />

## ClassNames you can override

You can pass a className to the root element to customize styles:

<Source
  language="js"
  code={dedent`<PaginationWithRangeInfo className="my-class-name" />`}
/>

Also, you can override styles using specific class names:

```
// Class for each select element
.Sui-PaginationWithRange--container {...}
```

See [Pagination](/?path=/docs/components-pagination--basic#classnames-you-can-override) for more classNames to override in the Pagination component.
