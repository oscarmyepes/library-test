import { Source } from '@storybook/addon-docs/blocks';
import { env } from '../config';

export const ImportStylesText = ({ componentName }) => (
  <>
    <h2>Import styles</h2>
    <p>
      To use the styled component, please import our styles. You can import
      styles for all components or only styles for {componentName}:
    </p>
    <ul>
      <li>
        <p>All styles:</p>
      </li>
    </ul>
    <Source
      language="js"
      code={`import "${env.LIBRARY_NAME}/build/index.css";`}
    />
    <ul>
      <li>
        <p>Only styles for this component:</p>
      </li>
    </ul>
    <Source
      language="js"
      code={`import "${env.LIBRARY_NAME}/build/${componentName}/index.css";`}
    />
  </>
);
